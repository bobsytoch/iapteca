---
apiVersion: v1
kind: Service
metadata:
  name: order-processing
  namespace: iapteca
  labels:
    app: order-processing
spec:
  selector:
    app: order-processing
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
